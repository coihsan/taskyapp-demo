import { Inter, Archivo } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const archivo = Archivo({
  subsets: ["latin"],
  display: "swap",
});
